---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="Page not found" description="The page you were looking for does not exist.">
  <section class="nf">
    <div class="container">
      <span class="code">404</span>
      <h1>This page doesn't exist</h1>
      <p class="lead">The page you were looking for isn't here, or it has moved. Head back home, or explore the current vWorkspace product.</p>
      <div class="btn-row nf__row">
        <a class="btn btn--primary" href="/">Back home</a>
        <a class="btn btn--secondary" href="/product/">Explore the product</a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .nf {
    padding-block: clamp(80px, 14vw, 180px);
    text-align: center;
  }
  .code {
    font-family: var(--font-mono);
    font-weight: 700;
    color: var(--brand-600);
    font-size: 18px;
  }
  .nf h1 {
    font-size: clamp(32px, 5vw, 48px);
    margin: 14px 0 16px;
  }
  .nf .lead {
    max-width: 46ch;
    margin-inline: auto;
  }
  .nf__row {
    justify-content: center;
    margin-top: 28px;
  }
</style>
