---
import BaseLayout from "../layouts/BaseLayout.astro";
import PageHero from "../components/PageHero.astro";
import CTA from "../components/CTA.astro";
import { links } from "../config";

const principles = [
  {
    title: "Customers own their stack",
    body: "One organization per installation. Customers own their data, identities, credentials, storage destinations, and deployment environment.",
  },
  {
    title: "Open platform, commercial delivery",
    body: "The platform is open and auditable. Commercial work covers dedicated pilots, hardware, installation, and support while customers keep control of their data.",
  },
  {
    title: "Security as a design constraint",
    body: "Clusters reach out in pull mode and the control plane does not hold a kubeconfig. Least privilege shapes the architecture.",
  },
  {
    title: "Built with pilot customers",
    body: "We develop from concrete operational friction with reference customers. Product direction, design, and tracking remain visible on GitHub.",
  },
];

const repos = [
  { name: "vworkspace", body: "Project overview, GTM docs, design spikes, and tracking.", href: links.vworkspace },
  { name: "vworkspace-server", body: "Control plane on Odoo 19 CE with the workspace UI, catalog, operations, knowledge, and AI experts.", href: links.server },
  { name: "vworkspace-operator", body: "Pull-mode Kubernetes operator in Go with CRDs, Flux, and Velero.", href: links.operator },
];
---

<BaseLayout
  title="About vWorkspace"
  description="vWorkspace connects company knowledge and daily IT operations in a private workspace with AI experts and human-approved changes."
>
  <PageHero
    eyebrow="Company"
    title="Making private workspaces useful to real organizations"
    text="vWorkspace connects company knowledge and daily operations with a team of AI experts and a supported operating model."
  />

  <section class="section section--tight">
    <div class="container">
      <span class="eyebrow">Principles</span>
      <div class="grid">
        {
          principles.map((p) => (
            <article class="card">
              <h3>{p.title}</h3>
              <p class="muted">{p.body}</p>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <section class="section section--alt">
    <div class="container">
      <span class="eyebrow">Built in the open</span>
      <h2 class="section-title">Three repositories, one product</h2>
      <div class="repos">
        {
          repos.map((r) => (
            <a class="repo" href={r.href} target="_blank" rel="noopener">
              <div>
                <code>{r.name}</code>
                <p class="muted">{r.body}</p>
              </div>
              <span class="repo__arrow">→</span>
            </a>
          ))
        }
      </div>
      <div class="links">
        <a href={links.governance} target="_blank" rel="noopener">Governance</a>
        <a href={links.contributing} target="_blank" rel="noopener">Contributing</a>
        <a href={links.security} target="_blank" rel="noopener">Security policy</a>
        <a href="/open-source/">Open-source overview</a>
      </div>
      <p class="muted license-note">
        Licensing is stated per repository. See the
        <a href="/open-source/">open-source overview</a> for the architecture,
        documentation, and commercial delivery model.
      </p>
    </div>
  </section>

  <CTA
    title="Work with vWorkspace"
    text="Evaluate knowledge search and workspace operations in a cloud or on-prem pilot, or review the open source project on GitHub."
    primaryLabel="Start a pilot"
    primaryHref={links.pilot}
    secondaryLabel="Explore on GitHub"
    secondaryHref={links.github}
  />
</BaseLayout>

<style>
  .grid {
    margin-top: 32px;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
  }
  .card {
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    padding: 24px;
  }
  .card h3 {
    font-size: 19px;
    margin-bottom: 10px;
  }
  .card p {
    font-size: 15px;
  }
  .repos {
    margin-top: 36px;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }
  .repo {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    padding: 18px 22px;
    transition: border-color 0.15s ease, transform 0.15s ease;
  }
  .repo:hover {
    text-decoration: none;
    border-color: var(--line-strong);
    transform: translateX(2px);
  }
  .repo code {
    font-size: 15px;
  }
  .repo p {
    margin-top: 6px;
    font-size: 14px;
  }
  .repo__arrow {
    color: var(--brand-600);
    font-size: 20px;
  }
  .links {
    margin-top: 32px;
    display: flex;
    flex-wrap: wrap;
    gap: 24px;
    font-weight: 500;
  }
  .license-note {
    margin-top: 20px;
    font-size: 14px;
    max-width: 62ch;
  }

  @media (max-width: 820px) {
    .grid {
      grid-template-columns: 1fr;
    }
  }
</style>
